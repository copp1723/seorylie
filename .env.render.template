# Render Environment Variables Template
# Copy these to your Render service environment settings

# Database (provided by Render PostgreSQL)
DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require

# Redis (provided by Render Redis)
REDIS_URL=redis://red-xxxxx:6379

# OpenRouter API
OPEN_ROUTER_API_KEY=your-openrouter-api-key

# JWT Secret for authentication
JWT_SECRET=your-secure-jwt-secret

# Node Environment
NODE_ENV=production

# API URLs (update based on your Render service URLs)
VITE_API_URL=https://your-backend-service.onrender.com
FRONTEND_URL=https://your-frontend-service.onrender.com

# Optional: Supabase (if using for other features)
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_KEY=your-service-key

# Optional: Monitoring
SENTRY_DSN=your-sentry-dsn-if-using

# Feature Flags
ENABLE_CHAT_TASKS=true
ENABLE_MANUAL_QUEUE=true
ENABLE_OPENROUTER_CACHE=true
